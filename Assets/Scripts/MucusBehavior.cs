using UnityEngine;
using System.Collections;
using UnityEngine.Events;

public class MucusBehavior : MonoBehaviour
{
    // Gabe: Code inspired by Playground Challenge code "ConditionArea"

	// This will be called EVERY FRAME when something stays inside the trigger collider

    /*
	void OnTriggerStay2D(Collider2D otherCollider)
	{
		//check for the tag of the object which entered the area, if necessary
		if(otherCollider.CompareTag("Player")
			|| otherCollider.CompareTag("Enemy"))
		{
            Debug.Log("Collided!");
			//otherCollider.gameObject.speed = 2;
		}
	}
*/
}